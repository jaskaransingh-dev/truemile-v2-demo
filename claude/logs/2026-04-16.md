# 2026-04-17

## What was completed

### Pass 2: Financials Tab + Expenses + KPIs
- Added 3 new Prisma models: ExpenseCategory, FleetExpense, FleetSettings
- Built fleet-expenses.routes.ts with 16 endpoints: expense categories CRUD + seed, fleet KPIs, driver KPIs, expenses CRUD + bulk create, expense file upload/parse (CSV/XLSX), fleet settings
- Registered routes at /api/fleet in index.ts
- Added 18 new API functions + 7 interfaces to dispatcher-api.ts
- Built 5 new mobile screens: FinancialsScreen, DriverFinancialsScreen, ManageCategoriesScreen, AddExpenseScreen, ExpenseUploadScreen
- Renamed Loads tab to Financials with DollarSign icon
- Added 4 new screens to DispatcherStack navigator

### Pass 2 Additions
- Editable driver names: long-press on DriversScreen opens edit modal, name field added to PUT endpoint
- Add Driver: "+ Add Driver" button with full modal (name, phone, truck#, trailer type, trailer#, home base, RPM), POST /api/dispatcher/drivers endpoint, optional Twilio SMS
- Load history month filter: month pills on DriverDetailScreen, backend ?month= query param on GET /drivers/:id/loads
- Cycle display rewrite: DriversScreen shows "Home by Apr 19" (tappable date picker), DriverDetailScreen replaced full cycle section with single "Home by" row + calendar icon

### Cleanup Fixes
- Month pills order: reversed to oldest→newest (Jan, Feb, Mar, Apr) on both DriverDetailScreen and FinancialsScreen
- Full Name field: added as first field in Edit Driver modal on DriverDetailScreen
- Replaced entire cycle section on DriverDetailScreen with simple "Home by [date]" row
- iOS date picker: wrapped in modal with Save/Cancel buttons, tap-outside-to-dismiss

### KPI Cards on FinancialsScreen
- 6 cards in 3 rows: Revenue, Avg RPM, Miles Driven, Utilization %, Fuel %, CPM
- Backend updated to compute and return: fuelExpenses, deadheadMiles, utilization, fuelPercent, cpm
- Layout: KPI cards → Driver Breakdown → Expenses → Upload buttons

## What's broken / in progress
- `npx prisma db push` needed — Supabase connection pool was saturated all session. Must run once pool frees up or after stopping backend process.
- Twilio not configured — SMS on driver creation will log warning and skip. Need TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_PHONE_NUMBER env vars.
- LoadsScreen.tsx still exists on disk but is no longer imported (replaced by FinancialsScreen). Can be deleted.

## Decisions made
- Factoring rate stored in FleetSettings model (default 2.2%), editable from FinancialsScreen header
- Expense categories seeded automatically on first load if none exist (24 defaults across FIXED/VARIABLE/DRIVER_PAY)
- Hard-coded Royal Carriers carrier/fleet IDs for driver creation — will be replaced with JWT-based lookup when multi-tenant auth is wired
- Cycle section removed entirely from DriverDetailScreen — replaced with single "Home by" date row matching DriversScreen display
- Month pills always ordered oldest→newest (Jan→current), default to current month
- Division by zero guard added on fleet expense split when drivers.length === 0

## Next session starts here
Run `npx prisma db push` to create the 3 new tables, then test the full Financials tab flow end-to-end on the mobile app.
